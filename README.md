### Your Energy — Курсовий проєкт

Навчальний вебзастосунок для відстеження та управління фізичними ресурсами.
Акцент зроблено на високій продуктивності, якісній архітектурі та сучасному
користувацькому інтерфейсі.

### 🚀 Технології

Основа проєкту — Vite, який забезпечує миттєве оновлення сторінок під час
розробки (HMR) та ефективну збірку.

- Середовище виконання: Node.js

- Інструмент збірки: Vite

- Мова програмування: JavaScript

- Стилі: Сучасна верстка на Flexbox/Grid із власними шрифтами (DMSans)

- Менеджер пакетів: npm

### 🛠 Основні можливості

1. Модульна організація: Код структуровано у каталозі src/, що спрощує
   розширення функціоналу.

2. Форми та зв'язок: Впроваджено обробку форм та функціонал відправлення
   email-повідомлень.

3. Оптимізація завантаження: Налаштовано ефективну роботу зі статикою, шрифтами
   та іконками для прискорення відкриття сторінок.

4. Культура коду: Застосовано .editorconfig для єдиного стилю написання та
   .gitignore для порядку у версіонуванні.

### 📦 Як встановити та запустити

Завантажте репозиторій:

```Bash
git clone https://github.com/your-username/YourEnergy-cp.git
```

Інсталюйте пакети:

```Bash
npm install
```

Увімкніть режим розробника:

```Bash
npm run dev
```

Створіть production-версію:

```Bash
npm run build
```

### 📂 Структура каталогів

```
.
├── src/            — код програми, компоненти та оформлення.
│   ├── css/        — стилі (reset, глобальні змінні, модулі).
│   ├── fonts/      — локальні шрифти (DMSans) для швидкого відображення.
│   ├── img/        — зображення та іконки з оптимізацією при збірці.
│   ├── js/         — логіка роботи: запити до API, валідація, інтерактивність.
│   ├── partials/   — HTML-фрагменти для модульної та зручної верстки.
│   ├── main.js     — центральна точка входу, що з'єднує модулі.
│   ├── index.html  — стартова сторінка та основа застосунку.
├── vite.config.js  — налаштування збірки.
└── package.json    — управління залежностями та командами.
```

Створено як курсова робота із застосуванням сучасних вебтехнологій.
